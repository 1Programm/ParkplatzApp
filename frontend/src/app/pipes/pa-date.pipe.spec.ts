import { ParkplatzDatePipe } from './pa-date.pipe';

describe('PaDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ParkplatzDatePipe();
    expect(pipe).toBeTruthy();
  });
});
